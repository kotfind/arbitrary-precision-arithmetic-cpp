# Simple arbitary-precision arithmetic library for C++
## Build
Building into `build` directory:
```
cmake -S . -B build
cmake --build build
```

## Run pi calculation
```
cmake --build build --target run
```

## Run tests
```
cmake --build build --target test
```
